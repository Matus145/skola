
import tkinter as tk
from tkinter import ttk

class Hotel:

    def __init__(self, druh_izby: str,cena: float, velkosť_m2: float, postele: int,
                 minibar: bool, klíma: bool, vaňa: bool):
        
        self.druh_izby = druh_izby
        self.cena = cena
        self.veľkosť = velkosť_m2
        self.postele = postele
        self.minibar = minibar
        self.klíma = klíma
        self.vaňa = vaňa

        
        pass
